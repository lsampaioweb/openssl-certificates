---
### This is just a sample file. Create import files using this one as an example.

# macoskeychain
keychain_path: "/Library/Keychains/System.keychain"
temp_folder: "/tmp"

# eclipse
secret_key: "KEY_STORE_PASSWORD"

# tplinkt1600g
port           : 443
tftp_folder    : "/private/tftpboot/"
tftp_app       : "com.apple.tftpd"
tftp_app_plist : "/System/Library/LaunchDaemons/tftp.plist"

import_config: [
  macoskeychain: [
    {
      host          : "MacBook-Pro-Luciano.local",
      keychain_path : "{{ keychain_path }}",
      temp_folder   : "{{ temp_folder }}"
    },
    {
      host          : "MacBook-Pro-Fernanda.local",
      keychain_path : "{{ keychain_path }}",
      temp_folder   : "{{ temp_folder }}"
    }
  ],
  eclipse: [
    {
      host        : "MacBook-Pro-Luciano.local",
      pkcs12_dest : "/opt/certificates/localhost/localhost.p12",
      secret_file : "/Users/lsampaio/.config/SpringBoot/secret.properties",
      secret_key  : "{{ secret_key }}"
    }
  ],
  tplinkt1600g: [
    {
      switch_id     : "T1600G-28TS",
      host          : "tplinkswitch",
      url           : "tplinkswitch.aprendendo.com.br",
      port          : "{{ port }}",
      tftp_folder   : "{{ tftp_folder }}",
      tftp_app      : "{{ tftp_app }}",
      tftp_app_plist: "{{ tftp_app_plist }}"
    }
  ]
]
