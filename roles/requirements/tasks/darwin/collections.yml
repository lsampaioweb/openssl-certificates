---
- name: "Installing Ansible Galaxy collections"
  ansible.builtin.import_role:
    name: "common"
    tasks_from: "darwin/packages/ansible-galaxy"
  vars:
    packages: "{{ ansible_galaxy_collections }}"
